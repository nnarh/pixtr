<h1>New Gallery</h1>

<%=form_for(@gallery) do |form| %>
  <%= render "errors", object: @gallery %>
  <%= render "errors", object: form.object %>
  <%= render form %>
  <%= form.submit "Create Gallery" %>
<% end %>

